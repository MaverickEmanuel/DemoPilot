# Where DemoPilot stores authored scripts and rendered artifacts.
# Default: ./.demopilot
DEMOPILOT_DATA_DIR=./.demopilot

# Set to "false" to run the browser headed (useful for debugging authoring).
# Default: headless
DEMOPILOT_HEADLESS=true
